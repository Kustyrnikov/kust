C = input("Введите символ C: ")
A = input("Введите строковую последовательность A: ").split()

count = sum(1 for s in A 
            if len(s) > 1 and s[0] == C and s[-1] == C)

print("Количество элементов А, которые содержат более одного символа и при этом начинаются и оканчиваются символом С:", count)
