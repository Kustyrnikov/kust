sequence = input("Введите строковую последовательность: ").split()

total_length = 0

for string in sequence:
    total_length += len(string)

print("Сумма длин всех строк, входящих в данную последовательность:", total_length)
