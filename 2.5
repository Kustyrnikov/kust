def meters_to_kilometers(meters):
    kilometers = meters / 1000
    print(f"Расстояние в километрах: {kilometers} км")

distance_in_meters = float(input("Введите расстояние в метрах: "))
meters_to_kilometers(distance_in_meters)
